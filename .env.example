# --- Admin login ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=use-a-strong-password
AUTH_SECRET=a-long-random-string-at-least-32-characters

# --- Vercel Postgres (Neon) ---
# Auto-added to your Vercel project when you create the Postgres store.
# For LOCAL dev, run `vercel env pull .env.local` after linking, or paste the
# connection string from the Vercel dashboard here.
POSTGRES_URL=

# --- Vercel Blob (image uploads) ---
# Auto-added when you create a Blob store. Pull locally with `vercel env pull`.
BLOB_READ_WRITE_TOKEN=
